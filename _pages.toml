# Cloudflare Pages 配置
# 前端构建产物目录: /dist

[build]
command = "cd frontend && npm run build"
cwd = "."
output_directory = "frontend/dist"

[build.environment]
NODE_VERSION = "18"

[[redirects]]
from = "/api/*"
to = "/api/:splat"
status = 200

[[redirects]]
from/*"
to = = " "/index.html"
status = 200
